<%= form_for(@widget, {url: lookup_choice_two_possibilities_widgets_path, method: :get, remote: true,  html: {id: 'widget-choice-one'}}) do |f| %>
  <% if @widget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@widget.errors.count, "error") %> prohibited this widget from being saved:</h2>

      <ul>
      <% @widget.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div id="widget-choice-one-fields" class="field">
    <label>Choice one</label><br/>
    <% @widget.choice_one_possibilities.each do |choice_one_possibility| %>
      <%= f.label :choice_one, choice_one_possibility %>
      <%= f.radio_button :choice_one, choice_one_possibility %><br/>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit "Submit Choice One", id: "widget-submit-choice-one" %>
  </div>
<% end %>

<%= render partial: "new_widget_form" %>

